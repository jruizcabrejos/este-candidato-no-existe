{
  "name": "rostro-promedio-congreso-2026",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:favicon": "node ./scripts/sync-favicon.mjs",
    "dev": "npm run sync:favicon && vite",
    "dev:full": "npm run generate && npm run sync:favicon && vite",
    "generate": "node ./scripts/generate-assets.mjs",
    "build": "npm run sync:favicon && vite build",
    "build:full": "npm run generate && npm run sync:favicon && vite build",
    "preview": "vite preview",
    "deploy": "powershell -ExecutionPolicy Bypass -File ./scripts/deploy.ps1"
  },
  "dependencies": {
    "gsap": "^3.14.2",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "sharp": "^0.34.5",
    "vite": "^8.0.3"
  }
}
